import { View, Text, } from "react-native";

export default function Card(){
    return(
        <View style={styles.container}>
            <Text>Add reusable component here for info, recipes, demo, etc...</Text>
        </View>
    )
}
